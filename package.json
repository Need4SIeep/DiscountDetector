{
  "name": "discount-detector",
  "version": "1.0.0",
  "description": "Cost Tracker - Household price comparison tool",
  "main": "backend/src/index.js",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm install --prefix backend",
    "heroku-postbuild": "npm install --prefix backend"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {},
  "devDependencies": {}
}
