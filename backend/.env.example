# Railway Configuration Guide

## Environment Variables (if needed)
PORT=5000
NODE_ENV=production

# No additional environment variables required for basic deployment
# The app will create and manage the SQLite database automatically
